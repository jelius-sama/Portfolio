package components

type LinkAttr struct {
	Href  string
	Class string
}

templ Link(attr LinkAttr) {
	<a href={ attr.Href } hx-get={ attr.Href } class={ attr.Class } hx-target="main" hx-swap="outerHTML" hx-push-url="true">
		{ children... }
	</a>
}
